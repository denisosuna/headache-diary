# Copia este archivo a `.env.local` y rellena con tus credenciales de Supabase.
# Si no se definen, la app guardará los datos en localStorage automáticamente.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
